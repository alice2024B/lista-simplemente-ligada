#include <iostream>

class lista{
  private:
  int date 100;
  int ult;
  public:
      lista(){
  if (ult==99)
    return true;
  return false;
  }
  bool interna(int dat, int pos){
      if llena()||pos<0 || pos>ult+1)

      }
  bool elimina(int pos)
  if(vacia() ||pos<0 || pos>ult)
    std::cout<<



using namespace std;

int main()
{
    cout << "Hello world!" << endl;
    return 0;
}
